spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/Quitomos/HelloWorld/
          searchPaths: spring-cloud-demo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
